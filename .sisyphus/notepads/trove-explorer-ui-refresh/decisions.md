
- 2026-07-25T15:49:44+08:00: Chose a targeted in-file layout consolidation in `src/pages/TroveExplorer.tsx` only (no helper extraction) to avoid coupling risk in this large page and to stay aligned with task scope.
- 2026-07-25T15:49:44+08:00: Promoted “Cross-Pane Search” to the primary compact header title and demoted “Trove Data Explorer” to a small uppercase support label, then moved import status chips/action + hidden-character toggle into the same unified top section.
- 2026-07-25T15:55:00+08:00: Accepted the compact-header task as conditionally verified in this environment after code review, LSP, build, and lint (full lint remains blocked by pre-existing `.references/yourddo/**` errors; project-source lint passes with `.references/**` ignored). Hands-on browser QA is deferred until a capable environment provides an installed browser and managed dev server.
- 2026-07-25T15:57:10+08:00: Moved the character selector out of the Character Bank pane header into a shared “Character Workspace” header above both character panes, preserving the existing `selectedCharacterId` state and all tab/item derivations (`characterBankTabs`, `inventoryTabs`) untouched.
- 2026-07-25T15:57:10+08:00: Adjusted the mid-page grid to two macro regions (Account pane + Character Workspace panel) so Character Bank and Character Inventory now read as a single character-focused workspace while preserving dense wrapped Stack control patterns and existing pane internals.
- 2026-07-25T16:02:29+08:00: Replaced Account pane mode `Tabs` with a compact `Storage` select in the pane header so users can still switch between Shared Account Bank and Crafting Storage while removing nested tab-style mode chrome from Crafting Storage itself.
- 2026-07-25T16:02:29+08:00: Removed separate `inventoryMode` state and merged Equipped into `normalizeInventoryTabs` output so bag tabs, Equipped, and optional Equipped Cosmetics live in one tab family (single sidebar list) instead of split sidebar/footer mode controls.
- 2026-07-25T16:06:38+08:00: Split account-pane data derivation by mode: Shared Account Bank continues to use `normalizeBankTabs(...)`, while Crafting Storage now uses `normalizeStorageItems(...)` (one synthetic "All Items" tab/page) and explicitly disables tab/page navigation chrome via `showTabNavigation={false}`.
- 2026-07-25T16:10:54+08:00: Kept the item-row compaction scoped to `TroveItemRow` so both pane lists and Cross-Pane Search inherit identical density/metadata composition, avoiding duplicate row variants and preserving existing inspect/wiki click behavior.
- 2026-07-25T16:10:54+08:00: Chose structural hover anchoring (querying an explicit `data-trove-hover-anchor` element and using that as Popper anchor) instead of pure offset tweaks, because overflow came from anchoring to full-row width rather than from offset magnitude.
