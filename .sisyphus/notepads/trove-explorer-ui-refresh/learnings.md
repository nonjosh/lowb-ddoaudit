
- 2026-07-25T15:49:44+08:00: Compacting Trove Explorer top chrome worked best by merging the old hero Paper and Cross-Pane Search Paper into a single dense block. This preserved all search interactions while materially reducing vertical space before pane content.
- 2026-07-25T15:49:44+08:00: Existing page style already favors wrapped Stack rows with dense controls; reusing that pattern (`direction="row"`, `useFlexGap`, `flexWrap="wrap"`) kept responsiveness consistent without touching pane logic.
- 2026-07-25T15:57:10+08:00: The cleanest way to communicate shared character scope was introducing a dedicated workspace wrapper around the Character Bank + Character Inventory panes while keeping the Account pane independent; this made the shared state explicit without changing any selection/data plumbing.
- 2026-07-25T15:57:10+08:00: Keeping compact per-pane identity chips (same selected name repeated in each pane header) retained quick context scanning and satisfied the requirement that both panes continue to expose active character context locally.
- 2026-07-25T16:02:29+08:00: `TroveStoragePane` is the right single source for pane chrome changes; converting its filter/search/sort block into one wrapped `Stack` automatically densified controls for Account, Character Bank, and Character Inventory together without per-pane branching.
- 2026-07-25T16:02:29+08:00: Trove equipped-cosmetic detection can be inferred safely from existing item fields by checking for "cosmetic" across `ItemType`, `ItemSubType`, `WeaponType`, and `EquipsTo`, which avoids schema changes while supporting a dedicated Equipped Cosmetics tab when data exists.
- 2026-07-25T16:06:38+08:00: The smallest safe way to make Crafting Storage non-tabbed was introducing a single-bank flattener (`normalizeStorageItems`) and reusing `TroveStoragePane` with `showTabNavigation={false}`; this preserves shared filtering/sorting/search behavior while removing both sidebar tabs and page footer chrome.
- 2026-07-25T16:10:54+08:00: Dense Trove rows are most readable when context (`locationLabel`) and metadata (`ML`, item type, `BTA/BTC`) are composed into a single compact subline with two-line clamp; this removes the old third metadata line while keeping cross-pane location context visible.
- 2026-07-25T16:10:54+08:00: Hover preview feels correctly attached when anchored to a left-side per-item marker (avatar wrapper) instead of the full-row ButtonBase; pairing that with viewport-constrained Popper (`flip` + `preventOverflow` alt axis + viewport boundary) prevents the detached far-right overflow behavior.
